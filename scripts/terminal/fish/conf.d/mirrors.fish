# ~/.config/fish/conf.d/mirrors.fish —— 国内镜像（uv/mise/npmmirror）
# 按需启用 / 修改镜像源

# uv（Python）
# set -gx UV_DEFAULT_INDEX https://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple

# mise/Node（npmmirror）
# set -gx NODEJS_BUILD_MIRROR https://npmmirror.com/mirrors/node

# Cargo（Rust）
# set -gx CARGO_NET_GIT_FETCH_WITH_CLI true
